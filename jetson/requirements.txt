# Disaster Sentinel — Python Dependencies
# Jetson Orin Nano (JetPack 6.x, Python 3.10+) & Cross-Platform Dev

# Web Frameworks & Dashboards
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
websockets>=12.0
jinja2>=3.1.2
python-multipart>=0.0.6
flask>=3.0.0

# Database
aiosqlite>=0.19.0
aiofiles>=23.2.1

# Configuration
pyyaml>=6.0.1

# Serial Communication (SIM800L)
pyserial>=3.5

# SPI Communication (LoRa) - Linux / Jetson hardware only
spidev>=3.6; sys_platform != 'win32'

# GPIO (Jetson)
# Jetson.GPIO is pre-installed on JetPack
# If running on non-Jetson for testing:
# RPi.GPIO>=0.7.1

# ML / AI / Deep Learning
numpy>=1.24.4
scikit-learn>=1.3.2
joblib>=1.3.2
pandas>=2.0.0
torch>=2.0.0

# Utilities
python-dateutil>=2.8.2
